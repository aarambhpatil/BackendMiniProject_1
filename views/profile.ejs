<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block">Logout</a>
        </div>
        <h2 class="text-3xl"><span class="font-light">Hello</span> <%= user.name %></h2>
        <h5 class="mb-5">You can create a Post</h5>
        <form method="post" action="/post">
            <textarea placeholder="What on your mind?" class="block w-1/3 outline-none p-3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"></textarea>
            <input class="px-3 w-40 py-2 bg-blue-500 block rounded-md mt-2" type="submit" value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h2 class="text-zinc-400">Your Posts.</h2>
            <div class="postcontainer mt-5">
                <% user.posts.forEach(function(post){ %>

                    <div class="post w-1/3 p-4 mb-4 rounded-md border[1px] border-zinc-700">
                        <h4 class="text-blue-500 mb-2"><%= user.username %></h4>
                        <p class="text-sm tracking-tight "><%= post.content %></p>
                        <div class="btns-flex gap-4">
                            <a class="text-blue-500" href="">Like</a>
                            <a class="text-zinc-600" href="">Edit</a>
                        </div>
                    </div>

                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>